const express = require('express')

const SubmitQuote = (req, res) => {
  // preprate req to save to db

  // save req => db table quote

  // if failed send res 500

  // send email to user and dyno

  // if failted send res 501 email failed

  // send success
}
